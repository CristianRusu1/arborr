# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Node (if added later)
node_modules/

# Environment / secrets — never commit these
.env
.env.local
.env.*.local
.dev.vars

# Cloudflare Wrangler build output (worker.js + wrangler.toml are tracked,
# but local build artifacts and dev-vars are not)
.wrangler/
dist/

# Claude Code local launch config (preview server, not needed in repo)
.claude/

# Python cache (from local SVG/PNG generation scripts)
__pycache__/
*.pyc
